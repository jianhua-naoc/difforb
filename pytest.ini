[pytest]
testpaths = tests
python_files = test_*.py
addopts = --strict-markers --import-mode=importlib -m "not slow"
xfail_strict = true
markers =
    slow: expensive tests that require explicit opt-in
